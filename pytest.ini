[pytest]
testpaths = tests
markers =
    unit: 单元测试
    integration: 集成测试（需要真实 API，默认跳过）
    slow: 耗时较长的测试
    regression: 回归测试
    semantic: 语义质量测试（需要 API）
    prompt_regression: Prompt 变更回归
addopts = -v --tb=short
